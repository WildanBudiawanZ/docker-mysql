# docker-mysql

### build the container

```
docker-compose up -d
```

### list the running container

```
docker container ls
```

### list all container

```
docker container ls -all
```

### list all images

```
docker images
```
